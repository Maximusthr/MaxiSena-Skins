# Copie para .env e preencha com suas credenciais locais

# Usado por src/database.py (psycopg2)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=maxisena
DB_USER=postgres
DB_PASSWORD=sua_senha

# Usado por src/app.py (Flask + SQLAlchemy)
DATABASE_URL=postgresql://postgres:sua_senha@localhost:5432/maxisena